{
  "name": "audio-ui",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "build": "turbo build",
    "build:pkg": "turbo build:pkg",
    "build:registry": "turbo build:registry",
    "serve:registry": "turbo serve:registry",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo build:pkg lint && changeset publish"
  },
  "packageManager": "bun@1.3.1",
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.0",
    "@manypkg/cli": "^0.25.1",
    "turbo": "2.5.8",
    "ultracite": "6.0.4"
  }
}
